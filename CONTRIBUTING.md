# Contribution Rules

## Update the bootstrap and docs

Any time you change or add dotfiles (including via AI agents), recheck and update:

- `bin/bootstrap`
- `README.md`

This keeps setup steps reproducible on a fresh machine.

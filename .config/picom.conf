opacity-rule = [
  "90:class_g = 'Terminator' && focused",
  "60:class_g = 'Terminator' && !focused"
];

blur-background: true;
blur-background-frame = true;
blur:
{
  method = "gaussian";
  size = 10;
  deviation = 5.0;
};
blur-background-exclude = [
    "class_g = 'i3-frame'",
];
